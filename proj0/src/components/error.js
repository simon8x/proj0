import React from 'react';

const Error = () => {
    return(
        <div className="error">
            <h2 className="subheader">Página no encontrada</h2>
            <p>La páginas no existe o temporalmente no se encuentra disponible</p>
        </div>
    );
}

export default Error